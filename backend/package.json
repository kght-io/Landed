{
  "name": "@landed/backend",
  "version": "0.0.0",
  "private": true,
  "description": "Server-only backend: the SQLite schema + queries, the job queue, agent orchestration, and on-disk asset paths. Imported by the frontend's route handlers \u2014 never by a client component.",
  "exports": {
    "./db": "./src/db/index.ts",
    "./*": "./src/*.ts"
  },
  "dependencies": {
    "@landed/shared": "*",
    "better-sqlite3": "^12.11.1",
    "drizzle-orm": "^0.45.2",
    "googleapis": "^173.0.0",
    "imapflow": "^1.4.3",
    "mailparser": "^3.9.12"
  }
}
